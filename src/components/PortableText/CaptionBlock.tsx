import React from "react";
import Typography from "@mui/material/Typography";

export const CaptionBlock = ({value}) => {
    return (
    <Typography
      component="div"
      style={{
      color: "#7C7B7B",
      cursor: "pointer",
      fontFamily: "'Lexend', sans-serif",
      textAlign: "center",
    }}
   /> 
)}